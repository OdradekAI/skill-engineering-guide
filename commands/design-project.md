---
description: "Plan a new bundle-plugin — use the bundles-forge:designing skill instead"
---

This command invokes the designing skill. Use it to plan a new bundle-plugin through structured interview.

Invoke the `bundles-forge:designing` skill to begin.
